# 博客写作与发布

## 日常使用

1. 打开 <https://app.pagescms.org>，使用 GitHub 登录。
2. 首次使用时，仅授权仓库 `LiuYaxuan33/LiuYaxuan33.github.io`。
3. 进入 **博客文章**，点击新建。
4. 填写标题、发布日期、摘要、标签和正文。图片可以直接拖入正文。
5. 写作期间保持 **立即发布** 关闭，点击保存即可存为草稿。
6. 准备发布时打开 **立即发布**，再次保存。

保存会直接在 GitHub 中留下版本记录；发布后的 GitHub Actions 通常会在几分钟内更新网站，不再需要本地 `commit` 或 `push`。

## 两点说明

- **文件短名** 只在新建文章时使用。请填写简短英文，例如 `berkeley-4`；系统会自动生成类似 `2026-09-19-berkeley-4.md` 的文件名。
- 草稿写了几天后再发布时，可以把 **发布日期** 改成真正发布的日期；页面日期和文章排序都会随之更新。
- **English body** 可以留空。中英文标题和摘要仍建议填写，保证博客列表切换语言时完整。

## 仍然可以本地编辑

后台直接修改仓库里的 Markdown 文件，因此 VS Code、Git 历史和 GitHub Actions 都照常可用。若后台与本地同时修改同一篇文章，先在本地执行 `git pull` 再继续，避免冲突。
